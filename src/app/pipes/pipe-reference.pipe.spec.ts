import { PipeReferencePipe } from './pipe-reference.pipe';

describe('PipeReferencePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeReferencePipe();
    expect(pipe).toBeTruthy();
  });
});
